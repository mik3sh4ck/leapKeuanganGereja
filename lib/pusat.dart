import 'package:flutter/material.dart';

class PusatApp extends StatefulWidget {
  const PusatApp({Key? key}) : super(key: key);

  @override
  State<PusatApp> createState() => _PusatAppState();
}

class _PusatAppState extends State<PusatApp> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(),
      ),
    );
  }
}
